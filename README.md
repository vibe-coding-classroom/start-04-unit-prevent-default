針對 **`start-04-unit-prevent-default` (防止瀏覽器預設行為：打造原生 App 體驗)** 單元，這是學員將網頁轉化為「專用設備」的技術轉捩點。它要求學員具備 **使用者經驗優化 (UX Optimization)** 與 **瀏覽器行為掌控 (Browser Constraint Mastery)** 的意識，學習如何透過 `event.preventDefault()` 消除移動端的捲動、縮放與右鍵選單，並利用 CSS 鎖定技術讓 Web App 的穩定性達到原生 App 的等級。

以下是在 **GitHub Classroom** 部署其作業 (Assignments) 的具體建議：

### 1. 範本倉庫 (Template Repo) 配置建議
防止預設行為作業的核心在於「穩定感與不被打斷的操作」，範本應包含一個「會亂動」的反面教材框架，建議包含：
*   **📂 `index.html`**：**原始遙控器介面**。故意不設定 Viewport 限制且不鎖定捲動，讓學員在手機上操作時會「邊滑邊跑路」，以此體會鎖定的必要性。
*   **📂 `js/ux.js`**：**事件攔截實作區**。學員在此實作 `touchmove` 的攔截邏輯。預留 `// TODO` 指導學員正確配置 `{ passive: false }`，這是現代瀏覽器中攔截捲動的成敗關鍵。
*   **📂 `style.css`**：**靜態鎖定核心區**。學員在此實作 `touch-action`、`user-select` 等屬性，解決點擊延遲與意外選取文字的問題。
*   **📂 `README.md`**：**UX 審計與相容性報告**。提供模板要求學員錄製或紀錄：在優化前，長按按鈕會發生什麼？優化後，快速雙擊是否還會放大頁面？

---

### 2. 作業任務部署細節

#### 任務 1：鎖住全螢幕捲動與橡皮筋回彈 (Viewport Lockdown Lab)
*   **目標**：打造「絕對靜止」的操作平面。掌握 `preventDefault()` 與 `{ passive: false }` 的底層配置邏輯。
*   **Classroom 部署建議**：
    *   **滾動攔截實作**：在 `document` 層級監聽 `touchmove`，並有條件地執行 `preventDefault()`。
    *   **CSS 配合挑戰**：對 `body` 或控制區加入 `overscroll-behavior: none` 以封印 iOS 常見的「底部回彈」效果。
    *   **驗證要點**：**操作穩定性審核**。學員是否理解了被動監聽器 (Passive Listener) 的限制？在操作區猛點、猛滑時，頁面是否依然穩如泰山？

#### 任務 2：打造像原生 App 的手勢鎖定區 (Native-Like Surface Lab)
*   **目標**：封印所有系統干擾。學會處理 iOS 與 Android 在長按、選取與選單觸發上的差異。
*   **Classroom 部署建議**：
    *   **系統選單攔截實作**：加入 `contextmenu` 攔截與 `-webkit-touch-callout: none`，防止遙控到一半跳出「儲存圖片」或「選取文字」的對話框。
    *   **點擊延遲優化**：應用 `touch-action: manipulation` 或 `none` 來消除瀏覽器預設的 300ms 雙擊等待時間，提升操控即時感。
    *   **驗證要點**：**跨平台相容性審核**。學員是否考慮了 iOS 專有的 CSS 屬性？按鈕上的文字是否已設定為無法選取？

#### 任務 3：緊急操作壓力測試 (Operational Stress Test Lab)
*   **目標**：驗證極端環境下的可靠性。確保在亂序、快速的多指操作下（模擬機器人驚險競速），介面不會位移或崩潰。
*   **Classroom 部署建議**：
    *   **壓力操作模擬**：要求學員在手機上同時使用三根手指進行暴力滑動，觀測是否有任何「露底」或頁面位移的情況。
    *   **介面完整性評估**：確認頁面佈局是否因為鎖定而犧牲了可讀性，並在 README 中寫下對「攔截」與「讓渡」權限的平衡心得。
    *   **驗證要點**：是否具備了處理邊界狀況的能力？提交的報告中是否有明確的對比數據或截圖？

---

### 3. UX 專業度與跨平台相容點評標準 (UX Polish Standards)
此單元的價值在於 **「對 Web 標籤感的剔除度與對原生觸感的還原度」**：
*   [ ] **操作專注度**：介面是否徹底封鎖了所有可能中斷使用者意圖的瀏覽器預設動作？
*   [ ] **參數精準度**：JS 事件監聽器中是否有根據現代規範正確設定 `passive` 參數？
*   [ ] **美學穩定性**：當系統彈出式選單被封印後，UI 是否因應手勢產生了對應的虛擬回饋補償？

### 📁 推薦範本結構 (GitHub Classroom Template)：
```text
.
├── index.html           # 結構：提供 Viewport Meta 設定與遙控按鈕佈局塊
├── style.css            # 樣式：練習 touch-action、user-select 與 overscroll 管理
├── js/ux.js             # 核心：練習 JS 層級的事件攔截與預設行為處理
├── README.md            # 總結：定義 UX 優化測試項目與不同裝置下的相容性報告
└── assets/test_guide.md # 參考：如何進行移動端壓力測試的標準流程
```

透過這種部署方式，學生能體驗到 **「好的前端工程師，不僅是會寫代碼的人，而是能剔除瀏覽器的冗餘雜訊、能重新定義網頁與指尖的物理關係、並在每一行 preventDefault 中，為使用者創造最純粹、最像原生 App 的控制靈魂大師」**。掌握防止預設行為技術後，學員將擁有了開發「專業級雲端遊戲控制器、精密醫療設備遠端介面、以及各類全螢幕互動看板」最核心也最資深的 UX 優化開發實力！_
---

# 🚀 學生作業實作區 (Student Assignment)

請依照以下任務指引，將此 Web App 的 UX 提升至「原生級別」。

## 任務清單
1.  **[ ] 任務 1：鎖住全螢幕捲動**
    - 在 `index.html` 修正 Viewport 設定。
    - 在 `js/ux.js` 正確實作 `touchmove` 攔截，並解決 `passive` 監聽器問題。
    - 在 `style.css` 加入 `overscroll-behavior: none`。
2.  **[ ] 任務 2：封印系統選單與選取**
    - 在 `style.css` 使用 `user-select: none` 與 `-webkit-touch-callout: none`。
    - 在 `js/ux.js` 攔截 `contextmenu`。
3.  **[ ] 任務 3：消除點擊延遲**
    - 在 `style.css` 為按鈕與搖桿加入 `touch-action: manipulation` 或 `none`。

## 📊 UX 審計與相容性報告

| 測試項目 | 優化前現象 (預設行為) | 優化後預期結果 | 實測狀態 |
| :--- | :--- | :--- | :--- |
| **長按按鈕** | 出現系統選單 (拷貝/查詢) | 無反應或自定義回饋 | [ ] |
| **快速雙擊** | 頁面放大 (Zoom in) | 頁面保持原樣 | [ ] |
| **搖桿滑動** | 觸發頁面捲動/橡皮筋回彈 | 僅移動搖桿，頁面靜止 | [ ] |
| **文字選取** | 按鈕文字被反白選取 | 文字不可選取 | [ ] |

## 📝 實作心得紀錄
- **遇到的挑戰**：(請在此描述你的解法)
- **跨平台觀察**：(iOS vs Android 的差異)
- **UX 哲學**：(為什麼這些細節對專業 Web App 很重要？)
